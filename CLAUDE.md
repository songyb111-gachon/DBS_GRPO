# 이 저장소에서 일하는 법

## 응답 규약

- **한국어로 답한다.** 커밋 메시지는 영어 산문 — 무엇을 왜 바꿨는지, 근거 숫자가 있으면 같이.
- 그림·다이어그램 안의 텍스트는 **영어**로. 렌더링 환경에 한글 글리프가 없어 깨진다.
  터미널 출력과 설명은 한국어로 해도 된다.
- 코드를 고쳤으면 **커밋하고 푸시까지** 한다. 커밋을 남겨 두고 사용자에게 시키지 않는다.

## 절대 제약

- `--force`, `reset --hard`, 히스토리 재작성 **금지.** 커밋 기록과 미커밋 변경을 잃는
  어떤 조작도 안 된다.
- **파일을 임의로 삭제하지 않는다.** 지워도 될 것 같으면 먼저 목록으로 제안하고 승인을 받는다.
  실제로 지울 때는 **마지막으로 존재하던 커밋 해시를 기록**해 `git checkout <해시> -- <경로>`
  로 되살릴 수 있게 한다.
- 되돌릴 때는 `git checkout -- <경로>` 같은 **비파괴 수단**을 쓴다.
- 큰 변경 전에는 직전 상태를 브랜치로 보존할지 묻는다.

## 설정은 코드에 박는다 — 플래그로 안내하지 않는다

**"이렇게 실행하세요 / 이 환경변수를 주세요" 라고 안내하지 말 것.** 값을 바꿔야 하면
**파일 안의 기본값을 직접 고쳐서 커밋**한다. 매번 플래그를 타이핑하는 걸 싫어한다.

- 설정은 **파일 상단 딕셔너리 하나**에 모으고, 모든 스크립트가 같은 모양을 갖는다.
- 고를 수 있는 값은 **옆 주석에 적어 둔다 — 복붙할 수 있게.**
- 주석에 적힌 값은 **"주석을 풀면 실제로 그 값이 적용된다" 는 약속**이다. 어긋나면 버그다.
- 덮어쓰기 계층이 여럿이면 순서를 명시하고, **실험의 정의에 해당하는 값**은 자동화가
  조용히 덮지 못하게 예외로 멈춘다.

## 작업 방식

- 개선 항목은 **번호 붙은 목록 파일**로 관리한다. 사용자는 번호로 지시한다 ("9 2 16 해줘").
- **버그는 즉시 고치고, 판단이 필요한 개선은 목록에 근거와 함께 적는다.** 둘을 섞지 말 것.
- **"구현은 해 두되 기본값으로는 켜지 마라"** 는 지시가 자주 온다. 설계 / 구현 / 활성화는
  **다른 결정**이다. 한 단계를 시켰는데 다음 단계까지 나가지 말 것.
- 넓게 위임하되 예외를 콕 집는다 ("X 빼고 다 진행"). **범위를 임의로 넓히지 말고**,
  하다가 발견한 건 고치지 말고 목록에 적는다.
- 작업 도중에 새 요구가 들어온다. **원래 작업을 버리지 말고 둘 다 끝낸다.**
- 진행 중인 비교 실험의 의미를 바꾸는 변경은 **스스로 유예하고 이유를 말한다.**
- 이미 문서에 조사·결론이 있으면 **다시 조사하지 않는다.** 재조사는 낭비로 본다.

## 설계 선호

- **조용한 폴백보다 큰 실패.** 설정이 무시되거나 기본값으로 조용히 도는 것이 최악이고,
  에러로 죽는 건 괜찮다. 그럴듯한 부분 결과가 나오는 상황을 만들지 말 것.
  폴백이 불가피하면 **최소 한 번은 이유를 출력**하게 한다.
- **손으로 맞춘 사본을 경계한다.** 같은 목록이 여러 파일에 복사되면 한쪽이 조용히 뒤처진다.
  원본에서 **생성**하거나, 불가피하면 **사본끼리 대조하는 검사**를 붙인다.
- 무거운 의존성 없이 수 초에 도는 **규약/일관성 검사 스크립트**를 두고, 고친 뒤 항상 돌린다.
  새 규약을 추가하면 검사기에도 항목을 추가한다.
- 주석에는 '무엇' 이 아니라 **왜 그 값인지와 측정값**을 남긴다.
- 이름·형식·기본값을 바꾸면 과거 산출물과의 비교가 끊긴다. **최소 변경**, 새 기능은
  **꺼진 채로** 들어가고, 이름은 **켰을 때만** 바뀌게 한다.
- 구조를 바꿨으면 **과거에 만든 산출물이 아직 읽히는지 확인해서 보고**한다.

## 보고 형식

- 문제 보고: **항목 / 왜 문제인지 / 안 고치면 나올 잘못된 결과 / 처리 여부.**
  근거는 `파일:줄` 로 단다.
- 현황 정리를 자주 요구한다: **끝난 것 / 남은 것 / 보류(왜) / 검증 결과 / 커밋·푸시 상태.**
- 여러 해결책이 있으면 **하나를 추천하고 이유를 댄다.** 선택지 나열로 끝내지 말 것.
- 성능 숫자는 **측정 조건과 기준선을 같이** 쓴다. 무처리 기준선 대비 개선이 없으면
  "학습/개선 안 됨" 으로 판정한다.
- **이 환경에서 확인 못 한 것은 추측하지 말고 "확인 필요" 로 분리**한다.
  검증한 것과 논증한 것을 섞지 말 것.
- **사용자가 반박하면 방어하지 말고 코드를 다시 확인한 뒤 사실대로 답한다.**
  반박이 맞는 경우가 잦다.
- **자기 실수는 발견 즉시 보고하고 고친다.** 숨기거나 축소하는 쪽이 훨씬 나쁘다.

## 도구 특성

- 로컬은 Windows + PowerShell(Git Bash 병행)이다. 경로에 한글이 들어갈 수 있다.
- `git commit -m "…"` 이 셸 인용 문제로 깨진 적이 있다 → **`git commit -F -` + 히어독**이 안전하다.
- **여러 파일을 한 번에 자동 편집할 때는 적용 전에 내용의 형식을 검증**할 것.
  실제로 두 번 오염된 적이 있다 — 이상하면 적용하지 말고 멈춘다.

## 원격 실행 환경

무거운 작업은 로컬이 아니라 **원격 서버**에서 사용자가 직접 돌린다.
로컬에는 GPU 도 무거운 런타임도 없을 수 있으므로, **의존성 없이 도는 분석·검사 수단**을 쓴다.

### ★ 편집은 로컬, 실행은 서버 노트북

**로컬에서는 노트북을 쓰지 않는다** — 로컬은 터미널이다. 노트북은 **서버에서만** 쓴다.
그런데 코드가 돌아가는 곳은 서버이므로, **노트북 제약은 코드가 지켜야 하는 제약**이다.
로컬에서 편집한다고 해서 그 제약을 빼면 서버에서 죽는다.

- **`__file__` 이 없는 실행을 고려한다.** 서버에서는 스크립트를 셀에 붙여넣거나
  `%run`·`exec` 로 부른다. 경로 계산을 `__file__` 에 기대면 죽고,
  `dirname(__file__) + '/..'` 같은 상대 조립은 **어느 폴더에서 부르느냐에 따라 엉뚱한
  곳**을 가리킨다. 저장소 루트는 **표식 파일을 찾아 올라가는 방식**으로 구하고
  `os.getcwd()` 로 폴백한다.
- **스크립트는 셀에 통째로 붙여넣어도 돌아야 한다.** 설정이 파일 상단 딕셔너리 하나에
  모여 있어야 하는 이유가 이것이다 — 그 값만 고쳐 바로 돌릴 수 있어야 한다.
- **앞 셀의 전역 변수가 파일 값을 이긴다.** 스윕·자동화가 그 경로로 값을 주입한다.
  접두사로 구분하고(예: `train__lr`), 덮였을 때는 **크게 찍어서** 알린다 —
  앞 셀의 잔재가 계속 이기는 사고가 실제로 난다.
- **셀 출력이 곧 사용자가 읽는 로그다.** 무엇을 골랐는지·무엇이 덮였는지·왜 멈췄는지를
  출력에 남긴다. 파일로만 남기면 셀에서 돌린 사람은 못 본다.

반대로 **로컬에서 무언가를 돌릴 때는 노트북 가정을 하지 않는다.** 터미널에서 도는
평범한 스크립트로 취급한다.

- ★ **원격 서버는 자동으로 pull 하지 않는다.** 장기 작업이 도는 동안 고친 코드는 서버에
  반영되지 않는다. **"고쳤으니 이제 된다" 고 보고하지 말고 pull 시점을 명시**할 것.
- ★ **자동화에 넣는 파라미터는 JSON 이다.** 파이썬 표기는 거절되고, 숫자는 양쪽 표기가
  같아서 **문자열·불리언이 든 항목만 조용히 실패**한다.
- **사용자별 동시 실행 상한**이 있어 제출한 작업이 한 번에 다 돌지 않는다.
  소요 시간을 추정할 때 반드시 반영할 것.
- 실행 산출물의 이름에 **잡·태스크 번호가 붙는다.** 이름으로 런을 식별할 수 있어야 한다.

## 사용자 배경

- 대학원 연구자다. **코드는 제품이 아니라 결과를 만드는 실험 장치**다.
- 측정 방법론에 엄격하다 — "최고점" 과 "마지막" 을 한 줄에 섞지 않고, 무처리 기준선을
  항상 함께 본다. 고정 예산으로 줄세우면 느린 설정이 나쁜 설정으로 오독된다는 것을 안다.
- **연구의 핵심 방법을 다른 방법으로 갈아타자는 제안을 거부한다.** 프록시 지표가 좋아져도
  목적은 연구 질문이다.
- **도메인 상수와 성능 손잡이를 구분한다.** 실험이 정하는 값을 성능을 위해 튜닝하려 하면
  거부당한다. 만지기 전에 어느 쪽인지 확인할 것.
