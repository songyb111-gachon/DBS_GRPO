# 학습 가능한 GRPO 학습 구조 설계 (v2) — 2026-09-06

연구 질문은 그대로다: **GRPO 로 학습한 정책이 DBS 의 "다음에 뒤집을 픽셀" 을 Random DBS 보다 잘 고르는가.**
이 문서는 그 정책이 실제로 배우게 만드는 *학습 구조*의 설계이고, 평가 프로토콜(고정 스텝 DBS 에서 PSNR 향상, Random 대비)은 바꾸지 않는다.
v2 는 `train_grpo.py` 의 `CONFIG["trainer"]` 로 고른다. 오라클·스모크 게이트를 통과한 2026-09-06 부터 기본값이 `"v2"` 다(`"v1"` 은 예전 경로, gymnasium 필요).

## 0. 성공 판정

| 판정 | 기준 | 어디서 |
|---|---|---|
| 학습됨 | 고정 검증 상태에서 정책이 고른 플립의 **실현 이득** E_π[R⁺] (R⁺ = max(ΔPSNR, 0)) 가 균등 무작위 E_unif[R⁺] 를 명확히 넘어 오르고, 개선 확률 P_π(R>0) > P_unif, **회수율** (E_π[R⁺] − E_unif[R⁺])/(top-1 − E_unif[R⁺]) ≥ 0.1 (0 = 균등, 1 = 오라클) | 학습 로그 `[v2 val]` 줄 (오라클로 정확히 계산, 시뮬레이션 불필요), `val.jsonl` |
| 이김 | 검증 이미지, 이미지당 고정 스텝(600) DBS 에서 GRPO 정책의 평균 PSNR 향상 > Random DBS, 이미지별 승수 과반 | `eval_checkpoints.py` (INCLUDE_RANDOM_BASELINE) — 프로토콜은 §2.5 |
| 천장 | 같은 조건의 **오라클 탐욕 DBS**(매 스텝 실제 최선 픽셀) 대비 회수율 | `eval_checkpoints.py` (INCLUDE_ORACLE_GREEDY) — 본 실험 표에 필수 |

무처리(초기 홀로그램) 대비 향상만으로는 판정하지 않는다. Random 을 못 넘으면 "학습/개선 안 됨".

**오라클이 있다는 것의 함의.** 학습 목적은 1스텝 ΔPSNR 이므로 그 최적 정책은 각 상태에서 argmax R, 곧 오라클 탐욕과 같은 선택이다. 정책이 그보다 "다른" 것을 배울 항은 설계에 없다. 그래서 (1) 오라클 탐욕은 스텝당 목적의 상한이고 정책의 성적은 그 회수율로 읽는다, (2) 정책의 존재 이유는 *시험 시 오라클 없이*(시뮬레이션 1회/스텝, 오라클 지도 없이) 그 선택을 재현하는 것이다 — 오라클 1회는 시뮬레이션 십수 회 값이라 정책이 회수율 r 을 내면 스텝당 비용 대비 이득으로 의미가 생긴다, (3) 오라클 지도는 정책 입력에 절대 넣지 않는다(답 누설). 연구 질문("학습 가능한가")은 그대로다: GRPO 가 이 감독 없는 보상만으로 그 선택을 배우는지가 실험이다.

## 1. 진단 — v1 구조가 못 배우는 이유

1. **보상이 비싸고 드물다.** 상태 하나에 G=16 개 샘플을 각각 전파 시뮬레이션(17회/스텝)하고, 그 16개로 4 epoch 를 돈다. 상태 간 배치가 없어 신호는 극히 작고 분산은 크다. 리셋마다 1만 회 시뮬레이션은 GRPO 학습에 쓰이지도 않는다(순위 보상은 로그용).
2. **정책이 답을 계산할 재료를 못 받는다.** 어떤 픽셀을 뒤집으면 좋아지는지는 *재생상 오차 × 복소 필드의 켤레* 를 전파 PSF 와 상관한 값으로 정해지는데(§2.1), 입력에는 세기(recon)만 있고 위상이 없다. 3×3 conv 5층(수용 영역 11px)은 PSF 폭(수백 px)에 한참 못 미친다.
3. **유효표본이 적다.** 상태당 16표본, 상태는 한 번에 하나. 어드밴티지의 그룹 통계도 그 16개에서 나온다.
4. **학습 신호와 로그가 어긋난다.** 로그의 `Reward=` 는 env 순위 보상이라 학습 진행을 판단할 수 없었다(1차 작업에서 PSNRgain 추가).

## 2. 설계

### 2.1 오라클 보상 엔진 — 모든 픽셀의 플립 효과를 한 번에, 정확히

`tt.simulate` 는 (100 패딩 → 중심화 FFT → 사각 주파수 마스크 × 대역제한 ASM 커널 → IFFT → 크롭) 이다. 즉 패딩 격자(456²) 위의 **선형·순환 이동불변 연산자 A** 다. 채널 c 의 필드 U_c = A h_c 이고, 픽셀 p 를 뒤집으면(부호 s_p = 1−2h_c(p)) U_c' = U_c + s_p·a_p (a_p = A e_p). 세기 I = (1/C)Σ_c|U_c|² 의 변화는
  D_p = (1/C)[ 2 s_p Re(conj(U_c) a_p) + |a_p|² ]
로 a_p 에 대해 정확히 2차다. relativeLoss(평균 정규화) 후 MSE 의 변화도 D_p 의 창 내 합들(Σ I·D_p, Σ T·D_p, Σ D_p, Σ D_p²)로 닫힌 형태가 되고, 각 합은 **PSF k 와의 순환 상관** 이므로 FFT 로 모든 p 에 대해 동시에 나온다. 채널당 FFT 십수 회.

- 정규화 계수 c' = mean(T)/(mean(I)+mean(D_p)) 의 변화까지 넣어 ΔMSE 를 **차이 형태로 직접** 조립한다(큰 수끼리 빼지 않음, float64 스칼라). 25~30 dB 에서는 2차 항(|a|² 계열)이 1차 항과 같은 크기이거나 더 크므로 근사할 수 없다.
- 커널 H 는 **반드시 `tt.get_ASM_kernel`·`tt.get_mask_by_range` 를 그대로 재사용**한다(직접 재유도 금지). 위상 2π z/λ ≈ 2.4e4 rad 이 float32 라 독립 구현은 1e-3 rad 어긋나 `tt.simulate` 와 달라진다.
- ΔPSNR = −10 log10(1 + ΔMSE/MSE).

**로컬 검증(완료)**: `grpo/oracle_algebra_check_np.py` — 같은 조립식을 numpy 로 옮겨 작은 격자(12², 패딩 5, 3채널)에서 432개 플립 전수를 float64 브루트포스와 대조. 최대 오차 5e-15 dB, 부호 일치 100%. 대수·roll/flip·크롭·2차 항이 맞다는 뜻이고, `tt.simulate` 와의 일치는 보장하지 않는다.

**게이트(서버 필수)**: `grpo/oracle_selftest.py` (합성 케이스 + BinaryNet 실제 케이스 각각):
(a) forward(h) == `tt.simulate(h, z)` 상대 오차 < 1e-5, (b) 수반 일치, (c) 오라클 PSNR == env 경로 PSNR(float32, 1e-4 dB) 및 == 같은 시뮬레이션의 float64 PSNR(1e-6 dB),
(d) 무작위 200 픽셀 + 오라클 최선/최악 각 8개의 ΔPSNR 을 브루트포스(실제 플립 후 시뮬레이션, **PSNR 은 float64 로 조립** — float32 PSNR 두 값의 차는 2e-6 dB 양자화 잡음을 갖는다)와 대조:
오차 ≤ 5e-6 + 0.01·|기준| (전 표본), 부호 일치 100% (|기준| > 1e-5 dB 인 표본만, 제외가 과반이면 FAIL), 피어슨·스피어만 > 0.999, (e) 속도.
한 픽셀 플립의 ΔPSNR 은 전형적으로 1e-4 dB 이므로 절대 허용오차 1e-4 같은 게이트는 틀린 오라클도 통과시킨다 — 그래서 상대 오차와 순위 상관을 본다.
**서버 결과 (2026-09-06, torch 2.7.1+cu128, torchOptics 8e50d6a)**: 합성·실제 케이스 모두 ALL PASS. 실제 케이스(BinaryNet 초기 홀로그램, DIV2K 검증 1장, PSNR 24.225 dB): forward == `tt.simulate` 오차 0, 오라클 vs 브루트포스 최대 오차 1.1e-7 dB(216 플립, 허용 1.5e-5), 부호 일치 100%(209/216 대상), 스피어만 0.999999, 지도 1회 4.4 ms. 지도 통계: P_unif(ΔPSNR>0) 20.8%, E_unif[R] −2.0e-4 dB, E_unif[R⁺] +2.7e-5 dB, top-1 +1.44e-3 dB — 무작위 플립의 실현 이득 대비 최선 플립이 54배이고, 600스텝 × E_unif[R⁺] ≈ 0.016 dB 는 Random DBS 의 실측 +0.02 dB/600스텝(ANALYSIS.md)과 자릿수가 맞는다.

**통과 전에는 v2 학습을 돌리지 않는다. 폴백은 없다** — 브루트포스 G 표본은 전체 지도 위에서만 정의되는 어드밴티지·검증 지표·상한 기준선을 줄 수 없어 "같은 인터페이스" 가 아니다. 실패하면 오라클을 고친다. 학습 시작 시에도 `startup_check` 가 첫 상태에서 8픽셀을 실제 시뮬레이션과 대조하고 불일치면 죽는다.

### 2.2 정책 입력과 구조

입력(모두 DBS 루프가 이미 계산하는 관측, `feature_spec="field"`, 51채널): state 8, pre_model 8, target 1, recon cI 1, 오차 E = cI − T 1, **복소 필드 Re/Im(Ũ_c) 16**, **곱 특징 Re/Im(E·conj Ũ_c) 16** (Ũ = U·√c). 곱 특징은 관측의 국소 함수라 누설이 아니다. 오라클의 1차 항은 이 곱을 고정 PSF 와 상관한 것이므로, 남는 과제는 대체로 "전역 고정 커널 하나 + 부호 + 2차 항" 이다. `"legacy"`(v1 의 26채널) 는 비교용이며 v2 에서는 state_record 채널이 항상 0 이다(평가도 같은 값을 넣는다).

- **상태 게이트** (`state_gate`, 기본 켬): 로짓 = h·L1 + (1−h)·L0. 뒤집을 방향의 부호를 관측 h 가 고르게 한다. 답 누설이 아니다(h 는 입력).
- `"fcn"` — v1 과 같은 5층 3×3 (비교용). `"unet"` — 4단 U-Net(수용 영역 수백 px), **기본 후보**. `"fno"` — 공유 스펙트럼 필터 1개 + 얕은 conv 헤드. 귀납 편향이 오라클 1차 항과 같아 PSF 회귀에 가깝다. **고정 반복 수로 unet 과 줄세우면 fno 가 이기는 것은 필연이므로, 그 비교는 "학습 속도" 비교로만 보고**하고 학습 가능성 주장은 unet 결과로 한다.

### 2.3 GRPO 배치 학습 루프 (v2)

- K 장의 이미지를 동시에 굴린다(각자 DBS 상태). 매 반복: 각 상태에서 U, I, 오라클 지도 R(a)(524,288개 전부)를 계산하고, 정책 π_old 에서 G 개 액션을 샘플(마스킹 불필요 — 실패는 보상이 말해 준다).
- **보상 변환** `reward_transform`: 기본 `"relu"` — R⁺ = max(R, 0). 평가 루프는 PSNR 이 오를 때만 상태를 갱신하므로 정책의 한 스텝 가치는 E_π[R⁺] 이지 E_π[R] 이 아니다(음수 크기는 실현되지 않는다). `"raw"` 는 비교 arm.
- **어드밴티지** `adv_baseline`: 기본 `"sample"` — G 표본의 평균·표준편차로 정규화 (DeepSeek GRPO 의 group-relative 그대로, v1 과 동일 정의). `"policy"` = π_old 가중 모집단 통계(G→∞ 극한, 표본 잡음 없음), `"uniform"` = 균등 모집단 통계(정책 무관 고정 기준 — GRPO 의 정의와 다르므로 비교 arm 으로만). 전체 지도가 있어 셋 다 공짜다.
- K×G 표본을 모아 E epoch 미니배치로 **clipped surrogate + KL(π‖π_ref)** 업데이트. π_ref 는 v1 과 같이 `ref_update_iters` 마다 갱신(0 = 고정 참조). 엔트로피 보너스는 옵션(기본 0; A 가 z-score 라 0.01 이하는 무의미).
- 상태 전진 `advance`: 기본 `"best"`(G 개 중 R>0 최고를 실행), `"sample"`(정책 샘플 1개 — 시험과 같은 분포). 채택 판정은 오라클 지도가 아니라 실제 재계산(forward + float64 PSNR, 엄격 증가)이고 거절 시 저장해 둔 상태·지도를 복원한다. 이미지당 `steps_per_image` 스텝 뒤 새 이미지. env.py 의 에피소드/임계 정의는 **학습에 쓰지 않는다**(평가에도 원래 안 쓰임). 체크포인트 축은 episode 가 아니라 iteration(`grpo_v2_it<N>.pt`) 이라 v1 과 이어지지 않는다.
- 진단 스위치 `objective: "grpo" | "exact"`: exact 는 Σ_a π(a)A(a) 를 직접 최대화(표본 분산 0, GRPO 첫 epoch gradient 의 모집단 기대값). **GRPO 가 아니므로** 결과를 GRPO 로 보고하지 않는다. 입력/구조 문제인지 분리하는 진단용이고 `adv_baseline="sample"` 과는 조합할 수 없다(에러).

### 2.4 학습 중 검증 지표 (시뮬레이션 없이 정확)

고정 검증 상태 V 개(검증 이미지 앞 V 장의 초기 홀로그램; 뒤 절반은 무작위-채택 DBS 로 `val_advance_steps` 진행한 중반 상태)에서 `val_every` 마다 한 줄:
E_π[R⁺], E_unif[R⁺], top-1 R, **회수율** (E_π[R⁺] − E_unif[R⁺])/(top-1 − E_unif[R⁺]), P_π(R>0), P_unif, E_π[R], E_unif[R], R(argmax π), 엔트로피, 유효 지지 크기 exp(H), 게이트 포화도. **학습됨 판정은 이 줄로 한다(§0).** `val.jsonl` 에 누적된다.

### 2.5 평가

`eval_checkpoints.py`/`test_grpo.py` 는 그대로 쓰되 v2 체크포인트를 읽는다(체크포인트의 policy_kind/feature_spec/state_gate/config 로 정책 복원). 본 실험 표에는 (1) Random 기준선, (2) **오라클 탐욕 상한**(`INCLUDE_ORACLE_GREEDY`) 을 넣고 각 행의 스텝당 시간도 함께 적는다(오라클 1회 vs 정책 1회 + 시뮬레이션 1회의 비용 비교).
**선택/보고 분리**: 같은 이미지에서 최고 체크포인트를 고르고 그 수치를 보고하면 위로 치우친다. `EVAL_SPLIT="select"`(앞 20장) 으로 체크포인트를 고르고 `"report"`(나머지) 로 보고한다. Random 은 확률적이므로 `EVAL_SEEDS=(0,1,2)` 로 시드 간 표준편차를 같이 적는다. 두 옵션 모두 기본은 꺼짐(지금까지의 출력과 동일).

### 2.6 바꾸지 않는 것

env.py 의 정의(max_steps, T_PSNR, T_PSNR_DIFF, num_samples), 광학 상수(`optics_constants.py`), 평가 지표와 프로토콜, 기존 v1 학습 경로(`trainer: "v1"` 로 선택 가능; v2 산출물은 별도 폴더 `grpo_models_v2_<policy_kind>/` 라 v1 산출물과 섞이지 않음), v1 체크포인트의 평가 결과(v1 은 레거시 GRPOPolicy 로 예전과 똑같이 읽는다).

## 3. 단계와 게이트

| 단계 | 파일 | 게이트 | 상태 |
|---|---|---|---|
| 1. 오라클 | `grpo/oracle.py`, `grpo/oracle_selftest.py`, `grpo/oracle_algebra_check_np.py` | 로컬 numpy 대수 검증 / 서버 selftest ALL PASS | 로컬 PASS / 서버 **PASS** (2026-09-06, §2.1 서버 결과) |
| 2. 특징·정책 | `grpo/features.py`, `grpo/policies.py` | `grpo/smoke_v2.py` 형상·유한성 | 서버 smoke PASS (2026-09-06, CUDA) |
| 3. 트레이너 v2 | `grpo/trainer_v2.py`, `grpo/dbs_state.py`, `train_grpo.py` CONFIG["v2"] | smoke 3조합(grpo/sample, grpo/policy, exact/uniform) + 체크포인트 저장·로드 | 서버 smoke PASS (2026-09-06, CUDA) |
| 4. 평가 지원 | `grpo/eval_utils.py`, `eval_checkpoints.py`, `test_grpo.py` | v1 체크포인트 결과 불변, v2 로드, 오라클 탐욕 행 | 구현, `check_conventions.py` 66/66 |
| 5. 서버 프로토콜 | — | (a) selftest → (b) smoke → (c) 짧은 런(수백 반복, K=4)에서 `[v2 val]` 의 E_π[R⁺] 가 E_unif[R⁺] 를 넘어 오르는지 → (d) 본 런 + `eval_checkpoints`(Random·오라클·split/seed) → (e) unet vs fno(학습 속도 비교) | 사용자가 서버에서 실행 |
| 6. 후속 | — | 학습되면 steps_per_image·G·K 스윕; 학습되는데 평가에서 Random 을 못 넘으면 advance="sample"·reward_transform 비교 | — |

버그마다 서버 왕복이 드는 것을 줄이기 위해 로컬에서 할 수 있는 것(대수 검증, 컴파일, 규약 검사, 두 차례 반박 검토)은 다 했다. 그래도 torch 코드 네 덩어리가 서버에서 처음 도는 것은 사실이라 (a)(b) 를 먼저, 짧게 돈다.

## 4. 위험과 대안

| 위험 | 신호 | 대안 |
|---|---|---|
| 오라클이 `tt.simulate` 와 안 맞음 | selftest 실패 | 오라클을 고친다(폴백 없음). 가장 가능성 큰 원인: 커널/마스크 규약 — `tt.get_ASM_kernel`·`get_mask_by_range` 재사용 여부, ifftshift 규약, 크롭 오프셋 |
| GPU float64 FFT 가 느려 "상태당 수십 ms" 가 안 나옴 | selftest 속도 줄 | 상관은 float32 FFT + float64 스칼라 조립이라 대부분 float32 다. 느리면 K 를 줄이고 steps_per_image 를 늘린다 |
| unet 이 안 배움 | E_π[R⁺] 가 E_unif 근처에서 정체, 회수율 < 0.05 | `objective="exact"` 로 입력/구조 문제인지 분리; fno arm 으로 입력이 충분한지 확인; lr·G 스윕 |
| 학습은 되는데 평가에서 Random 을 못 넘음 | 검증 지표 ↑, eval 승수 ↓ | 학습 상태 분포(best-of-G 전진) vs 시험(단일 샘플) 차이 → `advance="sample"`; 검증 상태에 중반 상태가 이미 포함돼 있으므로 그 줄과 대조 |
| 정책의 회수율이 낮아 의미가 의심됨 | 회수율 ≪ 1 | 연구 질문은 "학습 가능한가" 이므로 회수율과 Random 대비 승수를 그대로 보고; 탐욕은 상한으로만 |
| 선택 편향으로 승패가 뒤집힘 | select/report 결과가 다름 | §2.5 프로토콜(분리 + 시드) 로 보고 |

## 5. 결정 (소유자 확인)

오라클·스모크 게이트 통과(2026-09-06) 후 기본 트레이너를 v2 로 바꿨다(v1 은 CONFIG 한 줄로 복귀, 산출물 폴더 분리). 막히는 결정은 없으나 아래는 소유자의 판단이 필요하다.

1. **어드밴티지 기본값** `adv_baseline="sample"`(GRPO 정의 그대로). `"policy"`/`"uniform"` 은 비교 arm 으로만 둔다 — 동의하는지.
2. **보상 변환 기본값** `reward_transform="relu"`. 평가 지표와 같은 함수라는 이유이고, `"raw"` 는 비교 arm — 동의하는지.
3. **상태 게이트 기본 켬**(`state_gate=True`). 입력 h 로 부호를 고르는 구조적 장치라 "학습" 의 의미를 약화한다고 보면 끄고 돌린 결과도 함께 보고.
4. **fno 를 정책 후보에 넣는 것**: 물리 귀납 편향이 강해 결과는 "학습 속도 비교" 로만 보고한다는 조건 — 동의하는지.
5. **평가 프로토콜**: 본 실험 보고는 select(20장)/report(80장) 분리 + 시드 3개 — 동의하는지. (V=8, steps_per_image=200 은 제안값.)
6. **v1 체크포인트 축 단절**: v2 는 iteration 축이라 v1 의 ep 축과 같은 표에 놓이지 않는다(폴더도 다름) — 괜찮은지.
